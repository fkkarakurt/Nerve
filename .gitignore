# -- C build artifacts ----------------------------------------------------------
*.o
*.a
*.la
*.lo
*.so
*.so.*
*.dylib
*.dll
*.exe
*.out
*.app
*.hex

# -- C debug / dependency -------------------------------------------------------
*.d
*.pdb
*.idb
*.su
*.dSYM/

# -- Generated sources ----------------------------------------------------------
src/interpolation.c
generate_sigma

# -- Trained model files --------------------------------------------------------
*.net
*.bin

# -- Inference model artifacts (large; converted/downloaded, not source) --------
*.nrv
*.tok
*.nre
*.safetensors
studies/infer/tinyllama_config.json
studies/embed/minilm_config.json
studies/embed/vocab.txt
web/vocab.txt

# -- WebAssembly build outputs (generated by web/build.ps1) ---------------------
web/nerve.js
web/nerve.wasm
web/nerve.data

# -- npm package generated artifacts (shipped via package.json "files" on publish)
npm/nerve.mjs
npm/nerve.wasm
npm/nerve.data
npm/_t.mjs

# -- Python ---------------------------------------------------------------------
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
.env
venv/

# -- OS metadata ----------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# -- Editors / IDEs -------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
*.bak
*.orig
*.rej
*.sublime-project
*.sublime-workspace

# -- Profiling / tracing --------------------------------------------------------
callgrind.out.*
gmon.out
perf.data
perf.data.old

# -- Archives -------------------------------------------------------------------
*.tar.gz
*.tar.bz2
*.zip
*.7z

# -- Legacy test artifacts ------------------------------------------------------
Test
test.*
success_*
*.log

# -- CMake build artifacts ------------------------------------------------------
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
CPack*.cmake
_CPack_Packages/
install_manifest.txt
# generated .cmake files (keep CMakeLists.txt)
*.cmake
!CMakeLists.txt

# -- CMake out-of-source build directories --------------------------------------
build/
_build/
cmake-build-*/
out/

# -- Doxygen generated output (not source) -------------------------------------
docs/doxygen/

# -- LaTeX compiled artifacts (source .tex files ARE tracked) ------------------
docs/latex/*.aux
docs/latex/*.log
docs/latex/*.out
docs/latex/*.toc
docs/latex/*.lof
docs/latex/*.lot
docs/latex/*.bbl
docs/latex/*.blg
docs/latex/*.fdb_latexmk
docs/latex/*.fls
docs/latex/*.synctex.gz
docs/latex/*.pdf

# NOTE: do not add bare `web`, `site` or `*.html` rules here. They ignore the
# demo and docs *sources*, not just build output. The generated WebAssembly
# artifacts and model blobs are already covered above, by path and extension.

# -- OCR training data (large binaries -- download separately) ------------------
trainingData/optdigits.training/
trainingData/optdigits.validation/
trainingData/optdigits.wdependent/
trainingData/optdigits.windependent/

# -- MNIST IDX data (large binaries -- fetch with studies/mnist/download_data) --
studies/mnist/data/*-ubyte
studies/mnist/data/*.gz
run_log.txt

# -- Internal / not-public ------------------------------------------------------
ROADMAP.md

# -- Obsolete assets ------------------------------------------------------------
structure.png

# -- Legacy archive ----------------------------------------------------------
legacy_archive.zip
